# ============================================================
# SmartPark :: API Gateway — Bootstrap
# All configuration is served by config-server.
# ============================================================

spring:
  application:
    name: api-gateway

  config:
    import: "optional:configserver:http://localhost:8888"
